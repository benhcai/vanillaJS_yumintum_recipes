var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "getJSON", ()=>getJSON
);
// For functions used many times
const timeout = function(s) {
    return new Promise(function(_, reject) {
        setTimeout(()=>{
            reject(new Error(`💥💥 Request took took long: ${s}s`));
        }, s);
    });
};
// getJSON and Error handling
const getJSON = async function(url) {
    try {
        setTimeout(()=>{
            new Error(`💥💥 Request took took long: 1s`);
        }, 1);
        const timeoutPromsie = timeout(1);
        const res = await Promise.race([
            timeoutPromsie,
            fetch(url)
        ]);
        // const data = await res.json();
        // Check for valid url
        console.log(res);
        // if (!res.ok) throw new Error(`💥 !res.ok - ${data.message} ${res.status}`);
        // Destructure for recipe object and return
        return data.data.recipe;
    } catch (err) {
        console.error("catched: ", err);
    // throw err;
    }
};
